# 悦凯眼科服务后端 - 环境变量配置示例
# 
# 使用方法：
# 1. 复制此文件为 .env：cp env.example .env
# 2. 根据实际情况修改配置值
# 3. 注意：.env 文件不应提交到版本控制系统（已在 .gitignore 中）
#
# 配置优先级（从高到低）：
# 1. 系统环境变量
# 2. .env 文件（如果存在）
# 3. app/config.py 中的默认值

# ==================== 应用配置 ====================
# 调试模式（生产环境请设置为 False）
DEBUG=True

# API 版本前缀
API_V1_STR=/api/v1

# ==================== 数据库配置 ====================
# MySQL 主机地址
MYSQL_HOST=localhost

# MySQL 端口
MYSQL_PORT=3306

# MySQL 用户名
MYSQL_USER=root

# MySQL 密码（请修改为实际密码）
MYSQL_PASSWORD=your_password

# MySQL 数据库名
MYSQL_DATABASE=yuekai_ophthalmology

# ==================== Redis 配置 ====================
# Redis 主机地址
REDIS_HOST=localhost

# Redis 端口
REDIS_PORT=6379

# Redis 数据库编号
REDIS_DB=1

# Redis 密码（如果没有密码，留空）
REDIS_PASSWORD=

# ==================== AIGC 配置 ====================
# AIGC API 地址
AIGC_API_URL=https://aigc.x-see.cn

# AIGC API 密钥（请修改为实际密钥）
AIGC_API_KEY=your_aigc_api_key

# ==================== RQ 任务队列配置 ====================
# 任务超时时间（秒），默认 30 分钟
RQ_JOB_TIMEOUT=1800

# 任务结果保留时间（秒），默认 24 小时
RQ_JOB_RESULT_TTL=86400

# 失败任务保留时间（秒），默认 24 小时
RQ_JOB_FAILURE_TTL=86400

# 最大重试次数
RQ_JOB_RETRY_MAX=3

# 重试延迟（秒），默认 1 分钟
RQ_JOB_RETRY_DELAY=60

# ==================== 邀请码配置 ====================
# 有效的邀请码列表（多个邀请码用逗号分隔）
# 例如：INVITE_CODES=yuekaiyanke,code1,code2
INVITE_CODES=yuekaiyanke

# ==================== 短信验证码配置 ====================
# 短信验证码平台 URL
SMS_PLATFORM_URL=https://push.spug.cc/send/your_token

# 短信验证码平台发送方名称
SMS_PLATFORM_NAME=悦凯眼科

